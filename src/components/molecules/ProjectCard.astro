---
import type { TechTag } from '../../types/molecules';
import Button from '../atoms/Button.astro';

interface Props {
  title: string;
  description: string;
  tags: (TechTag | string)[];
  github: string;
  link?: string;
  image: string;
  images?: string[];
  index: number;
  codeLabel?: string;
  demoLabel?: string;
  class?: string;
}

const { title, description, tags, github, link, image, images, index, codeLabel = 'Code', demoLabel = 'Demo', class: className } = Astro.props;
const isEven = index % 2 === 0;
const gallery = images && images.length ? images : [image];

const getBadgeClass = (tag: TechTag | string): string => {
  if (typeof tag === 'string') {
    return 'bg-gray-200 text-gray-700 dark:bg-gray-700 dark:text-gray-300';
  }
  return tag.class || 'bg-gray-200 text-gray-700 dark:bg-gray-700 dark:text-gray-300';
};

const getBadgeName = (tag: TechTag | string): string => {
  if (typeof tag === 'string') return tag;
  return tag.name;
};
---

<article class:list={['w-full p-4 flex flex-col md:flex-row gap-x-8 gap-y-8', className]}>
  <div class="w-full md:w-1/2 md:max-w-lg">
    <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">{title}</h3>
    <ul class="flex gap-2 flex-wrap">
      {tags.map((tag) => (
        <li>
          <span class:list={['inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium', getBadgeClass(tag)]}>
            {getBadgeName(tag)}
          </span>
        </li>
      ))}
    </ul>
    <p class="text-base text-gray-600 dark:text-gray-300 mb-2 text-pretty">{description}</p>
    <footer class="flex items-end justify-start mt-4 gap-x-4">
      {github && github !== '#' && (
        <Button href={github}>{codeLabel}</Button>
      )}
      {link && (
        <Button href={link} variant="secondary" target="_blank" rel="noopener noreferrer">{demoLabel}</Button>
      )}
    </footer>
  </div>
  <div class="w-full md:w-1/2">
    <button
      type="button"
      data-lightbox-trigger
      data-lightbox-images={JSON.stringify(gallery)}
      aria-label={`Open image gallery for ${title}`}
      class="block w-full cursor-zoom-in p-0 bg-transparent border-0 focus:outline-none focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2 rounded-xl"
    >
      <img
        class:list={[
          'rounded-xl shadow-lg w-full h-48 object-cover transition-all duration-500 ease-in-out transform hover:scale-105',
          isEven ? 'hover:rotate-2' : 'hover:-rotate-2',
        ]}
        src={image}
        alt={`Screenshot of ${title}`}
      />
    </button>
  </div>
</article>